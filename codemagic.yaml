workflows:
  ios-workflow:
    name: Custom Workflow
    integrations:
      app_store_connect: codemagic
    environment:
      groups:
        - admin
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.hamid.arkit.new
    scripts:
      - name: Set up code signing settings on Xcode project
        script: |
          # explicitly point use‑profiles at your .xcodeproj
          xcode-project use-profiles \
            --project "/Users/builder/clone/arkitevideo.xcodeproj"
      - name: Verify schemes & show Build Settings
        script: |
          cd /Users/builder/clone
          # list available schemes
          xcodebuild -list \
            -project arkitevideo.xcodeproj
          # dump build settings to ensure everything’s wired up
          xcodebuild \
            -project arkitevideo.xcodeproj \
            -scheme arkitevideo \
            -showBuildSettings
      - name: Build App (.ipa) for App Store
        script: |
          cd /Users/builder/clone
          xcode-project build-ipa \
            --project "arkitevideo.xcodeproj" \
            --scheme "arkitevideo" \
            --configuration Release \
            --export-method app-store
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
